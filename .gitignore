# frontend
node_modules
dist

# rust
/src-tauri/target

# business/pricing strategy — local only, not for a repo anyone but the
# author can ever see (public or not: git history doesn't forget)
/docs/distribution.md

# licensing scheme write-up — end users of the program never need it, and
# it documents the license server's protocol in more detail than there's
# any reason to expose (the server itself, with the actual signing key,
# lives outside this repo entirely — see docs/licensing.md)
/docs/licensing.md

# tauri gen
/src-tauri/gen

# editors / OS
.DS_Store
*.log
